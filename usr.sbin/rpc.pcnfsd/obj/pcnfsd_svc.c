/*
 * Please do not edit this file.
 * It was generated using rpcgen.
 */

#include "pcnfsd.h"
#include <sys/ioctl.h>
#include <fcntl.h>
#include <stdio.h>
#include <err.h>
#include <stdlib.h>
#include <unistd.h>
#include <rpc/pmap_clnt.h>
#include <string.h>
#include <netdb.h>
#include <signal.h>
#ifdef __cplusplus
#include <sysent.h>
#endif /* __cplusplus */
#include <sys/types.h>
#include <memory.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <sys/resource.h>
#include <syslog.h>

#define SIG_PF void(*)(int)

#ifdef DEBUG
#define RPC_SVC_FG
#endif

#define _RPCSVC_CLOSEDOWN 120
#if 1
 static void _msgout(const char *);
 void msg_out(msg) const char *msg; {_msgout(msg);}
#endif
#if RPC_HDR
 extern void msg_out(const char *);
#endif
#if 1
#define main mymain
#endif
static int _rpcpmstart;		/* Started by a port monitor ? */
static int _rpcfdtype;		/* Whether Stream or Datagram ? */
static int _rpcsvcdirty;	/* Still serving ? */

static
void _msgout(const char *msg)
{
#ifdef RPC_SVC_FG
	if (_rpcpmstart)
		syslog(LOG_ERR, "%s", msg);
	else
		(void) fprintf(stderr, "%s\n", msg);
#else
	syslog(LOG_ERR, "%s", msg);
#endif
}

static void closedown(void);

static void
closedown(void)
{
	if (_rpcsvcdirty == 0) {
		static int size;
		int i, openfd;

		if (_rpcfdtype == SOCK_DGRAM)
			exit(0);
		if (size == 0) {
			struct rlimit rl;

			rl.rlim_max = 0;
			if (getrlimit(RLIMIT_NOFILE, &rl) == -1)
				return;
			if ((size = rl.rlim_max) == 0)
				return;
		}
		for (i = 0, openfd = 0; i < size && openfd < 2; i++)
			if (FD_ISSET(i, &svc_fdset))
				openfd++;
		if (openfd <= (_rpcpmstart?0:1))
			exit(0);
	}
	(void) alarm(_RPCSVC_CLOSEDOWN);
}

static void pcnfsdprog_1(struct svc_req *rqstp, SVCXPRT *transp);

static void
pcnfsdprog_1(struct svc_req *rqstp, SVCXPRT *transp)
{
	union {
		auth_args pcnfsd_auth_1_arg;
		pr_init_args pcnfsd_pr_init_1_arg;
		pr_start_args pcnfsd_pr_start_1_arg;
	} argument;
	char *result;
	xdrproc_t xdr_argument, xdr_result;
	char *(*local)(char *, struct svc_req *);

	_rpcsvcdirty = 1;
	switch (rqstp->rq_proc) {
	case PCNFSD_NULL:
		xdr_argument = (xdrproc_t)xdr_void;
		xdr_result = (xdrproc_t)xdr_void;
		local = (char *(*)(char *, struct svc_req *))pcnfsd_null_1_svc;
		break;

	case PCNFSD_AUTH:
		xdr_argument = (xdrproc_t)xdr_auth_args;
		xdr_result = (xdrproc_t)xdr_auth_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd_auth_1_svc;
		break;

	case PCNFSD_PR_INIT:
		xdr_argument = (xdrproc_t)xdr_pr_init_args;
		xdr_result = (xdrproc_t)xdr_pr_init_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd_pr_init_1_svc;
		break;

	case PCNFSD_PR_START:
		xdr_argument = (xdrproc_t)xdr_pr_start_args;
		xdr_result = (xdrproc_t)xdr_pr_start_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd_pr_start_1_svc;
		break;

	default:
		svcerr_noproc(transp);
		_rpcsvcdirty = 0;
		return;
	}
	(void) memset(&argument, 0, sizeof(argument));
	if (!svc_getargs(transp, xdr_argument, (caddr_t)&argument)) {
		svcerr_decode(transp);
		_rpcsvcdirty = 0;
		return;
	}
	result = (*local)((char *)&argument, rqstp);
	if (result != NULL && !svc_sendreply(transp, xdr_result, result)) {
		svcerr_systemerr(transp);
	}
	if (!svc_freeargs(transp, xdr_argument, (caddr_t)&argument)) {
		_msgout("unable to free arguments");
		exit(1);
	}
	_rpcsvcdirty = 0;
	return;
}

static void pcnfsdprog_2(struct svc_req *rqstp, SVCXPRT *transp);

static void
pcnfsdprog_2(struct svc_req *rqstp, SVCXPRT *transp)
{
	union {
		v2_info_args pcnfsd2_info_2_arg;
		v2_pr_init_args pcnfsd2_pr_init_2_arg;
		v2_pr_start_args pcnfsd2_pr_start_2_arg;
		v2_pr_queue_args pcnfsd2_pr_queue_2_arg;
		v2_pr_status_args pcnfsd2_pr_status_2_arg;
		v2_pr_cancel_args pcnfsd2_pr_cancel_2_arg;
		v2_pr_admin_args pcnfsd2_pr_admin_2_arg;
		v2_pr_requeue_args pcnfsd2_pr_requeue_2_arg;
		v2_pr_hold_args pcnfsd2_pr_hold_2_arg;
		v2_pr_release_args pcnfsd2_pr_release_2_arg;
		v2_mapid_args pcnfsd2_mapid_2_arg;
		v2_auth_args pcnfsd2_auth_2_arg;
		v2_alert_args pcnfsd2_alert_2_arg;
	} argument;
	char *result;
	xdrproc_t xdr_argument, xdr_result;
	char *(*local)(char *, struct svc_req *);

	_rpcsvcdirty = 1;
	switch (rqstp->rq_proc) {
	case PCNFSD2_NULL:
		xdr_argument = (xdrproc_t)xdr_void;
		xdr_result = (xdrproc_t)xdr_void;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_null_2_svc;
		break;

	case PCNFSD2_INFO:
		xdr_argument = (xdrproc_t)xdr_v2_info_args;
		xdr_result = (xdrproc_t)xdr_v2_info_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_info_2_svc;
		break;

	case PCNFSD2_PR_INIT:
		xdr_argument = (xdrproc_t)xdr_v2_pr_init_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_init_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_init_2_svc;
		break;

	case PCNFSD2_PR_START:
		xdr_argument = (xdrproc_t)xdr_v2_pr_start_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_start_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_start_2_svc;
		break;

	case PCNFSD2_PR_LIST:
		xdr_argument = (xdrproc_t)xdr_void;
		xdr_result = (xdrproc_t)xdr_v2_pr_list_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_list_2_svc;
		break;

	case PCNFSD2_PR_QUEUE:
		xdr_argument = (xdrproc_t)xdr_v2_pr_queue_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_queue_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_queue_2_svc;
		break;

	case PCNFSD2_PR_STATUS:
		xdr_argument = (xdrproc_t)xdr_v2_pr_status_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_status_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_status_2_svc;
		break;

	case PCNFSD2_PR_CANCEL:
		xdr_argument = (xdrproc_t)xdr_v2_pr_cancel_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_cancel_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_cancel_2_svc;
		break;

	case PCNFSD2_PR_ADMIN:
		xdr_argument = (xdrproc_t)xdr_v2_pr_admin_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_admin_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_admin_2_svc;
		break;

	case PCNFSD2_PR_REQUEUE:
		xdr_argument = (xdrproc_t)xdr_v2_pr_requeue_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_requeue_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_requeue_2_svc;
		break;

	case PCNFSD2_PR_HOLD:
		xdr_argument = (xdrproc_t)xdr_v2_pr_hold_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_hold_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_hold_2_svc;
		break;

	case PCNFSD2_PR_RELEASE:
		xdr_argument = (xdrproc_t)xdr_v2_pr_release_args;
		xdr_result = (xdrproc_t)xdr_v2_pr_release_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_pr_release_2_svc;
		break;

	case PCNFSD2_MAPID:
		xdr_argument = (xdrproc_t)xdr_v2_mapid_args;
		xdr_result = (xdrproc_t)xdr_v2_mapid_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_mapid_2_svc;
		break;

	case PCNFSD2_AUTH:
		xdr_argument = (xdrproc_t)xdr_v2_auth_args;
		xdr_result = (xdrproc_t)xdr_v2_auth_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_auth_2_svc;
		break;

	case PCNFSD2_ALERT:
		xdr_argument = (xdrproc_t)xdr_v2_alert_args;
		xdr_result = (xdrproc_t)xdr_v2_alert_results;
		local = (char *(*)(char *, struct svc_req *))pcnfsd2_alert_2_svc;
		break;

	default:
		svcerr_noproc(transp);
		_rpcsvcdirty = 0;
		return;
	}
	(void) memset(&argument, 0, sizeof(argument));
	if (!svc_getargs(transp, xdr_argument, (caddr_t)&argument)) {
		svcerr_decode(transp);
		_rpcsvcdirty = 0;
		return;
	}
	result = (*local)((char *)&argument, rqstp);
	if (result != NULL && !svc_sendreply(transp, xdr_result, result)) {
		svcerr_systemerr(transp);
	}
	if (!svc_freeargs(transp, xdr_argument, (caddr_t)&argument)) {
		_msgout("unable to free arguments");
		exit(1);
	}
	_rpcsvcdirty = 0;
	return;
}


int main(int, char *[]);

int
main(int argc, char *argv[])
{
	SVCXPRT *transp = NULL;
	int sock;
	int proto = 0;
	struct sockaddr_in saddr;
	socklen_t asize = (socklen_t)sizeof(saddr);

	if (getsockname(0, (struct sockaddr *)&saddr, &asize) == 0) {
		socklen_t ssize = (socklen_t)sizeof(int);

		if (saddr.sin_family != AF_INET)
			exit(1);
		if (getsockopt(0, SOL_SOCKET, SO_TYPE,
				(void *)&_rpcfdtype, &ssize) == -1)
			exit(1);
		sock = 0;
		_rpcpmstart = 1;
		proto = 0;
		openlog("pcnfsd", LOG_PID, LOG_DAEMON);
	} else {
#ifndef RPC_SVC_FG
		int size;
		struct rlimit rl;
		int pid, i;

		pid = fork();
		if (pid < 0) {
			err(EXIT_FAILURE, "cannot fork");
		}
		if (pid)
			exit(0);
		rl.rlim_max = 0;
		if (getrlimit(RLIMIT_NOFILE, &rl) == -1)
			err(EXIT_FAILURE, "getrlimit(RLIMIT_NOFILE)");
		if ((size = rl.rlim_max) == 0)
			exit(1);
		for (i = 0; i < size; i++)
			(void) close(i);
		i = open("/dev/console", 2);
		(void) dup2(i, 1);
		(void) dup2(i, 2);
		setsid();
		openlog("pcnfsd", LOG_PID, LOG_DAEMON);
#endif
		sock = RPC_ANYSOCK;
		(void) pmap_unset(PCNFSDPROG, PCNFSDVERS);
		(void) pmap_unset(PCNFSDPROG, PCNFSDV2);
	}

	if ((_rpcfdtype == 0) || (_rpcfdtype == SOCK_DGRAM)) {
		transp = svcudp_create(sock);
		if (transp == NULL) {
			_msgout("cannot create udp service.");
			exit(1);
		}
		if (!_rpcpmstart)
			proto = IPPROTO_UDP;
		if (!svc_register(transp, PCNFSDPROG, PCNFSDVERS, pcnfsdprog_1, proto)) {
			_msgout("unable to register (PCNFSDPROG, PCNFSDVERS, udp).");
			exit(1);
		}
		if (!svc_register(transp, PCNFSDPROG, PCNFSDV2, pcnfsdprog_2, proto)) {
			_msgout("unable to register (PCNFSDPROG, PCNFSDV2, udp).");
			exit(1);
		}
	}

	if ((_rpcfdtype == 0) || (_rpcfdtype == SOCK_STREAM)) {
		if (_rpcpmstart)
			transp = svcfd_create(sock, 0, 0);
		else
			transp = svctcp_create(sock, 0, 0);
		if (transp == NULL) {
			_msgout("cannot create tcp service.");
			exit(1);
		}
		if (!_rpcpmstart)
			proto = IPPROTO_TCP;
		if (!svc_register(transp, PCNFSDPROG, PCNFSDVERS, pcnfsdprog_1, proto)) {
			_msgout("unable to register (PCNFSDPROG, PCNFSDVERS, tcp).");
			exit(1);
		}
		if (!svc_register(transp, PCNFSDPROG, PCNFSDV2, pcnfsdprog_2, proto)) {
			_msgout("unable to register (PCNFSDPROG, PCNFSDV2, tcp).");
			exit(1);
		}
	}

	if (transp == NULL) {
		_msgout("could not create a handle");
		exit(1);
	}
	if (_rpcpmstart) {
		(void) signal(SIGALRM, (SIG_PF)closedown);
		(void) alarm(_RPCSVC_CLOSEDOWN);
	}
	svc_run();
	_msgout("svc_run returned");
	exit(1);
	/* NOTREACHED */
}
